# 請複製為 .env；兩個值都不可留空，且 .env 不可 commit。
VAULT_MASTER_PASSWORD=請改成一組長且唯一的主密碼
# 產生方式：python3 -c "import base64,os;print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
# 此金鑰用來加密伺服器上的保管庫；遺失或更換後，舊資料將無法解密。
VAULT_ENCRYPTION_KEY=請貼上產生的Fernet金鑰

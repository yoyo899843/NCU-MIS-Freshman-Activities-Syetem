POSTGRES_USER=stocksystem
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=stocksystem

# 用來簽發管理端與隊伍的 JWT，建議用一長串隨機字串（例如 openssl rand -hex 32）。
# 跟 Time-Space Warfare、RPG System 的 JWT_SECRET 各自獨立，不要共用同一組。
JWT_SECRET=change-me-to-a-long-random-string

POSTGRES_USER=timewarfare
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=timewarfare

# 用來簽發 /admin 的 JWT，建議用一長串隨機字串（例如 openssl rand -hex 32）
JWT_SECRET=change-me-to-a-long-random-string
